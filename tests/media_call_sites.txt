# Media call-site audit -- coverage gate input for tests/test_media_call_sites.py.
#
# Every uncommented `path:line` below is a genuine `.media` access on an
# Object/Upload/Rewrite domain object that the conformance trace
# (trace_rest + trace_grpc + trace_faults) executes today. The gate fails if
# any listed line is NOT executed by the trace, turning the ~71-site `.media`
# audit from a claim into a check. Later tasks migrate these sites onto the
# Media interface and add their new lines here.
#
# Lines were captured from real subprocess coverage of the gunicorn worker
# (see .coveragerc / testbench/__init__.py) and reviewed by hand:
# `media_link` matches and docstring/comment mentions of `.media` were dropped.
#
# `# UNCOVERED path:line -- reason` records a genuine media site the trace does
# NOT exercise today, so the gap stays visible rather than silently dropped.
# These are comments: the gate skips them, so they do not turn it red.
#
# Task 5 re-pin: routing REST resumable + gRPC WriteObject/BidiWrite (non-
# appendable) uploads onto a store-provided staging Media inserted new
# `upload.media = db.store.new_upload_media(...)` factory sites (gcs/upload.py:192
# and :409, grpc_server.py:1244, rest_server.py:1095) and shifted subsequent line
# numbers; every entry below is content-verified against the working tree.

# --- gcs/object.py ---
gcs/object.py:67
gcs/object.py:91
gcs/object.py:129
gcs/object.py:135
gcs/object.py:136
gcs/object.py:137
gcs/object.py:503
gcs/object.py:527
gcs/object.py:534
gcs/object.py:555
gcs/object.py:557
gcs/object.py:568
# gcs/object.py:67 is `media.chunks(...)` in the module-level `_stream_media`
# helper; gcs/object.py:568 is the `instructions is None` REST download streamer
# passing `self.media` into it. Together they are the normal-download streaming
# path (Task 6 made this the ONLY non-fault download path -- normal AND ranged --
# and dropped the old unranged `to_bytes()` hot path at object.py:535-536).
# gcs/object.py:503 is `_download_range`'s `len(self.media)`, now computed
# arithmetically (no payload slicing) so a FileMedia is never materialised to
# clamp a range; :534 is `response_payload = self.media` (the non-transcode
# payload stays the Media); :527 is the transcode `self.media.reader()`.
# gcs/object.py:129 is `Object.init`'s own media-wrap (`media if isinstance(...)
# else BytesMedia(media)`) and :135 the size assignment that follows it -- the
# classmethod every insert/compose/rewrite/finalize path uses to construct a
# new Object. gcs/object.py:555/:557 are the widened fault-injection escape
# hatch: `isinstance(response_payload, Media)` (was BytesMedia at old :535)
# taking its `.to_bytes()` branch. Task 6 narrowed this to the fault/instruction
# path only (`instructions is not None`), so it now fires solely on the faults
# trace's small, unranged instruction downloads (begin=0), never on the normal
# or ranged path.
# UNCOVERED gcs/object.py:128 -- corrupt_media() call gated on the `inject-upload-data-error` instruction; the trace never sends that instruction into Object.init
# UNCOVERED gcs/object.py:671 -- the default `else` REST download streamer's self.media access; the trace issues no download whose testbench instructions fall through to this branch

# --- gcs/upload.py ---
gcs/upload.py:192
gcs/upload.py:244
gcs/upload.py:249
gcs/upload.py:267
gcs/upload.py:279
gcs/upload.py:409
gcs/upload.py:564
gcs/upload.py:569
gcs/upload.py:587
gcs/upload.py:602
gcs/upload.py:604
gcs/upload.py:655
gcs/upload.py:696
gcs/upload.py:714
gcs/upload.py:715
# gcs/upload.py:192 and :409 are the Task-5 staging factory sites:
# `upload.media = db.store.new_upload_media(...)` for WriteObject
# (init_write_object_grpc's write_object_spec branch) and BidiWrite's
# non-appendable write_object_spec branch respectively. :279/:602 are the
# `upload.media += content` gRPC accumulate statements (O_APPEND on FileMedia);
# :696 is the BidiWrite non-appendable finalize `Object.init(..., upload.media, ...)`
# -- the `.to_bytes()` escape hatch dropped in Task 5.
# UNCOVERED gcs/upload.py:245 -- partial-media resend branch (write_offset < persisted size); the gRPC write trace never re-sends already-persisted bytes
# UNCOVERED gcs/upload.py:255 -- OUT_OF_RANGE abort (write_offset > persisted size); the trace never sends a gap-ahead offset
# UNCOVERED gcs/upload.py:311 -- _insert_empty_appendable_object; the trace performs no appendable-object upload
# UNCOVERED gcs/upload.py:565 -- partial-media resend branch, BidiWrite variant of :245
# UNCOVERED gcs/upload.py:575 -- OUT_OF_RANGE abort, BidiWrite variant of :255
# UNCOVERED gcs/upload.py:626 -- update_appendable_blob closure (checksummed_data flush); no appendable upload in the trace
# UNCOVERED gcs/upload.py:627 -- same update_appendable_blob ternary as :626 (the BytesMedia-passthrough value); no appendable upload in the trace
# UNCOVERED gcs/upload.py:628 -- same update_appendable_blob ternary as :626 (the isinstance condition); no appendable upload in the trace
# UNCOVERED gcs/upload.py:629 -- same update_appendable_blob ternary as :626 (the BytesMedia-wrap else branch); no appendable upload in the trace
# UNCOVERED gcs/upload.py:631 -- update_appendable_blob closure size assignment; no appendable upload in the trace
# UNCOVERED gcs/upload.py:672 -- finalize_blob closure (appendable finalize); no appendable upload in the trace
# UNCOVERED gcs/upload.py:673 -- same finalize_blob ternary as :672 (the BytesMedia-passthrough value); no appendable upload in the trace
# UNCOVERED gcs/upload.py:674 -- same finalize_blob ternary as :672 (the isinstance condition); no appendable upload in the trace
# UNCOVERED gcs/upload.py:675 -- same finalize_blob ternary as :672 (the BytesMedia-wrap else branch); no appendable upload in the trace
# UNCOVERED gcs/upload.py:680 -- finalize_blob closure size assignment; no appendable upload in the trace

# --- testbench/grpc_server.py ---
# Line numbers re-pinned in Task 9 after `import os` was inserted into the
# import block (one line above every media site here); the sites themselves are
# unchanged (content-verified). `_bind_host()` sits below all of them, near
# `run`, so it shifts nothing here. Task 5 added the StartResumableWrite factory
# site :1244 and shifted QueryWriteStatus :1249 -> :1254.
testbench/grpc_server.py:551
testbench/grpc_server.py:616
testbench/grpc_server.py:617
testbench/grpc_server.py:625
testbench/grpc_server.py:647
testbench/grpc_server.py:785
testbench/grpc_server.py:804
testbench/grpc_server.py:1096
testbench/grpc_server.py:1129
testbench/grpc_server.py:1189
testbench/grpc_server.py:1192
testbench/grpc_server.py:1194
testbench/grpc_server.py:1197
testbench/grpc_server.py:1200
testbench/grpc_server.py:1219
testbench/grpc_server.py:1244
testbench/grpc_server.py:1254
# grpc_server.py:551 (ComposeObject) and :1096 (single-shot Rewrite)
# accumulate via `BytesMedia().append(src.media.to_bytes())`; the `.to_bytes()`
# is Plan 5's explicit escape hatch. grpc_server.py:1194 is the
# incremental-rewrite `rewrite.media.append(...)` statement, which black wraps
# across :1194-:1196 -- coverage attributes the whole statement to its opening
# line :1194, so the appended `src_object.media[...]` slice on :1195 is covered
# there and is not listed separately. grpc_server.py:1129 is WriteObject's
# finalize `Object.init(..., upload.media, ...)` -- the `.to_bytes()` dropped in
# Task 5; :1244 is `upload.media = self.db.store.new_upload_media(...)` in
# StartResumableWrite (the resumable-gRPC staging factory).
# UNCOVERED testbench/grpc_server.py:651 -- broken-stream fault chunk in ReadObject's chunks() loop; the trace injects no broken-stream fault on a gRPC read
# UNCOVERED testbench/grpc_server.py:689 -- broken-stream fault chunk on ReadObject's trailing empty response; needs a 0-byte or exact-size-multiple read combined with a broken-stream fault, which the trace never does
# UNCOVERED testbench/grpc_server.py:784 -- negative read_offset in BidiReadObject; the trace uses no negative offset on a bidi read
# UNCOVERED testbench/grpc_server.py:1125 -- resumable-but-unfinished WriteObject early return; the trace's WriteObject always sets finish_write

# --- testbench/rest_server.py ---
# Line numbers re-pinned in Task 9 after the env-driven store selection
# (`import os` + the `_init_db_from_env` helper) was inserted near the top of
# the module, above every media site here; the sites themselves are unchanged
# (content-verified). Task 5 added the resumable staging factory site :1095 and
# shifted the resumable-chunk media sites below it.
testbench/rest_server.py:105
testbench/rest_server.py:739
testbench/rest_server.py:789
testbench/rest_server.py:858
testbench/rest_server.py:860
testbench/rest_server.py:865
testbench/rest_server.py:866
testbench/rest_server.py:869
testbench/rest_server.py:870
testbench/rest_server.py:879
testbench/rest_server.py:1095
testbench/rest_server.py:1114
testbench/rest_server.py:1131
testbench/rest_server.py:1161
testbench/rest_server.py:1183
testbench/rest_server.py:1296
testbench/rest_server.py:1297
testbench/rest_server.py:1308
# rest_server.py:1095 is the Task-5 staging factory
# `upload.media = db.store.new_upload_media(...)` right after init_resumable_rest;
# :1296 is the resumable-chunk `upload.media += data` (O_APPEND on FileMedia) and
# :1308 the finalize `Object.init(..., upload.media, ...)` -- the `.to_bytes()`
# dropped in Task 5.
# UNCOVERED testbench/rest_server.py:1189 -- finalize a chunked resumable upload via a `bytes */N` PUT matching persisted size; the trace does not finalize on this branch
# UNCOVERED testbench/rest_server.py:1301 -- non-Content-Range simple-upload completion; the trace's resumable uploads always send a Content-Range

# --- testbench/filestore.py ---
# UNCOVERED testbench/filestore.py:179 -- object_inserted link_into's a still-growing appendable FileMedia into its destination; only under TESTBENCH_STORE=file, and the coverage gate runs the memory-config conformance trace (NullStore), which never constructs a FileStore
# UNCOVERED testbench/filestore.py:183 -- object_inserted finalize's a one-shot FileMedia (O(1) promote) into its destination; only under TESTBENCH_STORE=file, and the coverage gate runs the memory-config conformance trace (NullStore), which never constructs a FileStore
# UNCOVERED testbench/filestore.py:186 -- object_inserted persists a BytesMedia via to_bytes()+write_bytes_atomic (fallback); only under TESTBENCH_STORE=file, and the coverage gate runs the memory-config conformance trace (NullStore), which never constructs a FileStore

# --- testbench/common.py (fault-injection append helpers) ---
# UNCOVERED testbench/common.py:994 -- handle_stall_uploads_after_bytes; the trace injects no stall-after-bytes fault
# UNCOVERED testbench/common.py:1027 -- handle_retry_uploads_error_after_bytes partial commit; the trace's REST fault paths miss this after-bytes window
# UNCOVERED testbench/common.py:1029 -- handle_retry_uploads_error_after_bytes error trigger; same helper as :1027
# UNCOVERED testbench/common.py:1053 -- handle_grpc_retry_uploads_error_after_bytes guard; the trace injects no gRPC error-after-bytes fault
# UNCOVERED testbench/common.py:1055 -- handle_grpc_retry_uploads_error_after_bytes range_end; same helper as :1053
# UNCOVERED testbench/common.py:1057 -- handle_grpc_retry_uploads_error_after_bytes partial commit; same helper as :1053
# (testbench/common.py:1051 is a docstring mention of `upload.media`, not a call site -- dropped)
