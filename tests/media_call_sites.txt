# Media call-site audit -- coverage gate input for tests/test_media_call_sites.py.
#
# Every uncommented `path:line` below is a genuine `.media` access on an
# Object/Upload/Rewrite domain object that the conformance trace
# (trace_rest + trace_grpc + trace_faults) executes today. The gate fails if
# any listed line is NOT executed by the trace, turning the ~71-site `.media`
# audit from a claim into a check. Later tasks migrate these sites onto the
# Media interface and add their new lines here.
#
# Lines were captured from real subprocess coverage of the gunicorn worker
# (see .coveragerc / testbench/__init__.py) and reviewed by hand:
# `media_link` matches and docstring/comment mentions of `.media` were dropped.
#
# `# UNCOVERED path:line -- reason` records a genuine media site the trace does
# NOT exercise today, so the gap stays visible rather than silently dropped.
# These are comments: the gate skips them, so they do not turn it red.
#
# Task 5 re-pin: routing REST resumable + gRPC WriteObject/BidiWrite (non-
# appendable) uploads onto a store-provided staging Media inserted new
# `upload.media = db.store.new_upload_media(...)` factory sites (gcs/upload.py:192
# and :409, grpc_server.py:1244, rest_server.py:1095) and shifted subsequent line
# numbers; every entry below is content-verified against the working tree.

# --- gcs/object.py ---
gcs/object.py:67
gcs/object.py:78
gcs/object.py:107
gcs/object.py:145
gcs/object.py:151
gcs/object.py:152
gcs/object.py:153
gcs/object.py:519
gcs/object.py:549
gcs/object.py:550
gcs/object.py:556
gcs/object.py:576
gcs/object.py:580
gcs/object.py:587
gcs/object.py:591
# gcs/object.py:67 is `media.chunks(...)` in the module-level `_stream_media`
# helper and :78 is `media.reader()` in the module-level `_stream_transcode`
# helper (Task 7). :591 is the `instructions is None` REST download streamer
# passing `self.media` into `_stream_media` (the ONLY non-fault normal AND ranged
# path -- Task 6 dropped the old unranged `to_bytes()` hot path); :587 is the same
# streamer's decompressive-transcode branch passing `self.media` into
# `_stream_transcode`. Task 7 streams the transcode with a bounded two-pass
# counted Content-Length -- :550 is the counting pass
# (`sum(len(buf) for buf in _stream_transcode(self.media))`), so neither the
# normal nor the transcode download ever materialises the whole object.
# gcs/object.py:519 is `_download_range`'s `len(self.media)`, computed
# arithmetically (no payload slicing) so a FileMedia is never materialised to
# clamp a range; :556 is `response_payload = self.media` (the non-transcode
# payload stays the Media) and :549 the transcode branch's `response_payload =
# self.media`.
# gcs/object.py:145 is `Object.init`'s own media-wrap (`media if isinstance(...)
# else BytesMedia(media)`) and :151/:152/:153 the size/md5/crc32c reads that
# follow it -- the classmethod every insert/compose/rewrite/finalize path uses to
# construct a new Object; :107 is the matching wrap in `Object.__init__`.
# gcs/object.py:576/:580 are the widened fault-injection escape hatch:
# `isinstance(response_payload, Media)` (was BytesMedia) taking its `.to_bytes()`
# branch. Task 6 narrowed this to the fault/instruction path only (`instructions
# is not None`), so it now fires solely on the faults trace's small, unranged
# instruction downloads (begin=0), never on the normal or ranged path.
# UNCOVERED gcs/object.py:144 -- corrupt_media() call gated on the `inject-upload-data-error` instruction; the trace never sends that instruction into Object.init
# UNCOVERED gcs/object.py:578 -- the transcode fault path materialising the gunzipped bytes (b"".join(_stream_transcode(self.media))); the trace injects no fault on a decompressive-transcode download
# UNCOVERED gcs/object.py:692 -- the default `else` REST download streamer's transcode branch (_stream_transcode(self.media)); the trace issues no download whose testbench instructions fall through to this branch
# UNCOVERED gcs/object.py:694 -- the default `else` REST download streamer's non-transcode branch (_stream_media(self.media)); the trace issues no download whose testbench instructions fall through to this branch

# --- gcs/upload.py ---
gcs/upload.py:192
gcs/upload.py:244
gcs/upload.py:249
gcs/upload.py:267
gcs/upload.py:279
gcs/upload.py:409
gcs/upload.py:564
gcs/upload.py:569
gcs/upload.py:587
gcs/upload.py:602
gcs/upload.py:604
gcs/upload.py:655
gcs/upload.py:696
gcs/upload.py:714
gcs/upload.py:715
# gcs/upload.py:192 and :409 are the Task-5 staging factory sites:
# `upload.media = db.store.new_upload_media(...)` for WriteObject
# (init_write_object_grpc's write_object_spec branch) and BidiWrite's
# non-appendable write_object_spec branch respectively. :279/:602 are the
# `upload.media += content` gRPC accumulate statements (O_APPEND on FileMedia);
# :696 is the BidiWrite non-appendable finalize `Object.init(..., upload.media, ...)`
# -- the `.to_bytes()` escape hatch dropped in Task 5.
# UNCOVERED gcs/upload.py:245 -- partial-media resend branch (write_offset < persisted size); the gRPC write trace never re-sends already-persisted bytes
# UNCOVERED gcs/upload.py:255 -- OUT_OF_RANGE abort (write_offset > persisted size); the trace never sends a gap-ahead offset
# UNCOVERED gcs/upload.py:311 -- _insert_empty_appendable_object; the trace performs no appendable-object upload
# UNCOVERED gcs/upload.py:565 -- partial-media resend branch, BidiWrite variant of :245
# UNCOVERED gcs/upload.py:575 -- OUT_OF_RANGE abort, BidiWrite variant of :255
# UNCOVERED gcs/upload.py:626 -- update_appendable_blob closure (checksummed_data flush); no appendable upload in the trace
# UNCOVERED gcs/upload.py:627 -- same update_appendable_blob ternary as :626 (the BytesMedia-passthrough value); no appendable upload in the trace
# UNCOVERED gcs/upload.py:628 -- same update_appendable_blob ternary as :626 (the isinstance condition); no appendable upload in the trace
# UNCOVERED gcs/upload.py:629 -- same update_appendable_blob ternary as :626 (the BytesMedia-wrap else branch); no appendable upload in the trace
# UNCOVERED gcs/upload.py:631 -- update_appendable_blob closure size assignment; no appendable upload in the trace
# UNCOVERED gcs/upload.py:672 -- finalize_blob closure (appendable finalize); no appendable upload in the trace
# UNCOVERED gcs/upload.py:673 -- same finalize_blob ternary as :672 (the BytesMedia-passthrough value); no appendable upload in the trace
# UNCOVERED gcs/upload.py:674 -- same finalize_blob ternary as :672 (the isinstance condition); no appendable upload in the trace
# UNCOVERED gcs/upload.py:675 -- same finalize_blob ternary as :672 (the BytesMedia-wrap else branch); no appendable upload in the trace
# UNCOVERED gcs/upload.py:680 -- finalize_blob closure size assignment; no appendable upload in the trace

# --- testbench/grpc_server.py ---
# Line numbers re-pinned in Task 8: ComposeObject's `composed_media = BytesMedia()`
# (1 line) grew to a staging-factory call (3 lines) and its
# `composed_media.append(src.media.to_bytes())` (1 line) grew to a chunked
# streaming loop (6 lines incl. comment), shifting every site below by +7. The
# sites themselves are content-verified against the working tree. (Task 9 re-pin
# after `import os` and Task 5's StartResumableWrite factory :1251 predate this.)
testbench/grpc_server.py:557
testbench/grpc_server.py:623
testbench/grpc_server.py:624
testbench/grpc_server.py:632
testbench/grpc_server.py:654
testbench/grpc_server.py:792
testbench/grpc_server.py:811
testbench/grpc_server.py:1103
testbench/grpc_server.py:1136
testbench/grpc_server.py:1196
testbench/grpc_server.py:1199
testbench/grpc_server.py:1201
testbench/grpc_server.py:1204
testbench/grpc_server.py:1207
testbench/grpc_server.py:1226
testbench/grpc_server.py:1251
testbench/grpc_server.py:1261
# grpc_server.py:557 (ComposeObject) is the Task-8 streaming site
# `for chunk in source_blob.media.chunks(0, len(source_blob.media), size):` --
# the `.to_bytes()` accumulate is gone, each source now streams a read chunk at
# a time into the staging Media (coverage attributes the loop to its `for` line).
# :1103 (single-shot Rewrite) still accumulates via
# `BytesMedia().append(src.media.to_bytes())`; the `.to_bytes()` is the explicit
# escape hatch Task 9 removes. grpc_server.py:1201 is the incremental-rewrite
# `rewrite.media.append(...)` statement, which black wraps across :1201-:1203 --
# coverage attributes the whole statement to its opening line :1201, so the
# appended `src_object.media[...]` slice is covered there and not listed
# separately. grpc_server.py:1136 is WriteObject's finalize
# `Object.init(..., upload.media, ...)` -- the `.to_bytes()` dropped in Task 5;
# :1251 is `upload.media = self.db.store.new_upload_media(...)` in
# StartResumableWrite (the resumable-gRPC staging factory).
# UNCOVERED testbench/grpc_server.py:658 -- broken-stream fault chunk in ReadObject's chunks() loop; the trace injects no broken-stream fault on a gRPC read
# UNCOVERED testbench/grpc_server.py:696 -- broken-stream fault chunk on ReadObject's trailing empty response; needs a 0-byte or exact-size-multiple read combined with a broken-stream fault, which the trace never does
# UNCOVERED testbench/grpc_server.py:791 -- negative read_offset in BidiReadObject; the trace uses no negative offset on a bidi read
# UNCOVERED testbench/grpc_server.py:1132 -- resumable-but-unfinished WriteObject early return; the trace's WriteObject always sets finish_write

# --- testbench/rest_server.py ---
# Line numbers re-pinned in Task 8: `import uuid` was added to the import block
# (+1 to every site below), and objects_compose's `composed_media = b""` +
# `composed_media += source_object.media` (materialising idiom) became a
# staging-factory call plus a chunked streaming loop -- net +1 across the
# compose block, so sites below it shift by +2 total. The sites are
# content-verified against the working tree. (Task 9's env-driven store selection
# and Task 5's resumable staging factory :1097 predate this.)
testbench/rest_server.py:106
testbench/rest_server.py:740
testbench/rest_server.py:791
testbench/rest_server.py:860
testbench/rest_server.py:862
testbench/rest_server.py:867
testbench/rest_server.py:868
testbench/rest_server.py:871
testbench/rest_server.py:872
testbench/rest_server.py:881
testbench/rest_server.py:1097
testbench/rest_server.py:1116
testbench/rest_server.py:1133
testbench/rest_server.py:1163
testbench/rest_server.py:1185
testbench/rest_server.py:1298
testbench/rest_server.py:1299
testbench/rest_server.py:1310
# rest_server.py:740 (objects_compose) is the Task-8 streaming site
# `for chunk in source_object.media.chunks(0, len(source_object.media), size):`
# -- the `b"" += source.media` materialising idiom is gone, each source streams
# a read chunk at a time into the staging Media (coverage attributes the loop to
# its `for` line). :1097 is the Task-5 staging factory
# `upload.media = db.store.new_upload_media(...)` right after init_resumable_rest;
# :1298 is the resumable-chunk `upload.media += data` (O_APPEND on FileMedia) and
# :1310 the finalize `Object.init(..., upload.media, ...)` -- the `.to_bytes()`
# dropped in Task 5.
# UNCOVERED testbench/rest_server.py:1191 -- finalize a chunked resumable upload via a `bytes */N` PUT matching persisted size; the trace does not finalize on this branch
# UNCOVERED testbench/rest_server.py:1303 -- non-Content-Range simple-upload completion; the trace's resumable uploads always send a Content-Range

# --- testbench/filestore.py ---
# UNCOVERED testbench/filestore.py:179 -- object_inserted link_into's a still-growing appendable FileMedia into its destination; only under TESTBENCH_STORE=file, and the coverage gate runs the memory-config conformance trace (NullStore), which never constructs a FileStore
# UNCOVERED testbench/filestore.py:183 -- object_inserted finalize's a one-shot FileMedia (O(1) promote) into its destination; only under TESTBENCH_STORE=file, and the coverage gate runs the memory-config conformance trace (NullStore), which never constructs a FileStore
# UNCOVERED testbench/filestore.py:186 -- object_inserted persists a BytesMedia via to_bytes()+write_bytes_atomic (fallback); only under TESTBENCH_STORE=file, and the coverage gate runs the memory-config conformance trace (NullStore), which never constructs a FileStore

# --- testbench/common.py (fault-injection append helpers) ---
# UNCOVERED testbench/common.py:994 -- handle_stall_uploads_after_bytes; the trace injects no stall-after-bytes fault
# UNCOVERED testbench/common.py:1027 -- handle_retry_uploads_error_after_bytes partial commit; the trace's REST fault paths miss this after-bytes window
# UNCOVERED testbench/common.py:1029 -- handle_retry_uploads_error_after_bytes error trigger; same helper as :1027
# UNCOVERED testbench/common.py:1053 -- handle_grpc_retry_uploads_error_after_bytes guard; the trace injects no gRPC error-after-bytes fault
# UNCOVERED testbench/common.py:1055 -- handle_grpc_retry_uploads_error_after_bytes range_end; same helper as :1053
# UNCOVERED testbench/common.py:1057 -- handle_grpc_retry_uploads_error_after_bytes partial commit; same helper as :1053
# (testbench/common.py:1051 is a docstring mention of `upload.media`, not a call site -- dropped)
